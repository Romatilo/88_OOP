package seminar.transport;

public class Car extends Vehicle {

    public Car(String vehicleColor, String vehicleModel, int vehicleWeight, int vehicleWheelsAmount, int vehicleMaxSpeed) {
        super(vehicleColor, vehicleModel, vehicleWeight, vehicleWheelsAmount=4, vehicleMaxSpeed);
    }
}
